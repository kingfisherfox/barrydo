{
  "name": "barrydo",
  "version": "1.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply barrydo --local",
    "migrate": "wrangler d1 migrations apply barrydo --remote",
    "types": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260903.1",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.129.0",
    "zod": "^3.25.0"
  }
}
